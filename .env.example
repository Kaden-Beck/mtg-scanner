# Copy to .env and adjust as needed. docker-compose.yml reads this file.

# Path to the SQLite database file (must be on a persistent volume in Docker).
DATABASE_PATH=./data/mtg.db

# Host port to publish the web app on.
WEB_PORT=3000
